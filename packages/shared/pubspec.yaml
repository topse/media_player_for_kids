name: shared
description: Gemeinsame Datenklassen und Interfaces

resolution: workspace

environment:
  sdk: ">=3.10.4 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  dart_couch_widgets:
    git:
      url: https://github.com/topse/dart_couch
      path: packages/dart_couch_widgets

  dart_mappable: ^4.7.0
  watch_it: ^2.4.1
  logging: ^1.3.0

dev_dependencies:
  flutter_lints: ^6.0.0
  build_runner: ^2.10.5
  dart_mappable_builder: ^4.7.0
